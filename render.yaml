services:
  - type: web
    name: django-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python manage.py migrate
    startCommand: gunicorn django_rev.wsgi
    envVars:
      - key: DEBUG
        value: false
      - key: ALLOWED_HOSTS
        value: yourdomain.onrender.com
